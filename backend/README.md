# Density Backend

Django REST API for the Density mobile app.

## Endpoints

- `GET /api/health/`
- `GET /api/home/`

## Environment

Use the root `.env.example` as the template. The Docker Compose service reads `.env` from the repository root.
